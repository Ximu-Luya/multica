---
title: Assign tasks to digital employees
description: Hand a task to a digital employee and it takes over as the official assignee until the work is done — with full context and the ability to change task status and fields.
---

import { Callout } from "fumadocs-ui/components/callout";

Assign a [task](/issues) to a [digital employee](/agents) and it works as the **official assignee** until the work is done — it can read the full task context (description + all [comments](/comments)) and change status, post comments, and edit fields. This is the **most common and heaviest** of Multica's four trigger paths. The same flow also accepts a [digital department](/squads) as the assignee — Multica then triggers the digital department's **leader digital employee** instead.

| Path | When to use | Changes the task | Context | Priority | Auto retry |
|---|---|---|---|---|---|
| **Assign** | Hand a digital employee ownership | Changes assignee | Task + all comments | Inherits from task | ✓ |
| [**@-mention**](/mentioning-agents) | Pull it in to take a look | No changes | Task + trigger comment | Inherits from task | ✓ |
| [**Chat**](/chat) | One-to-one conversation outside any task | No task involved | Current conversation history | Fixed medium | ✓ |
| [**Autopilots**](/autopilots) | Scheduled or manual automation | Depends on mode | Depends on mode | Set by autopilot | ✗ |

"Auto retry" refers to retries after infrastructure failures (runtime offline, timeout). Business errors on the digital employee side (for example, the model reporting an error) are not retried. See [**Tasks**](/tasks) for details.

## Assign from the UI

On the task detail page, click the **Assignee** picker. It lists every member in the workspace, all non-archived digital employees, and every non-archived [digital department](/squads). Pick a digital employee (or digital department) and the task is assigned right away.

A few rules:

- **Workspace digital employees** can be assigned by any member; **private digital employees** can only be assigned by their owner or a workspace admin.
- You can only assign to digital employees that have **an online runtime** — digital employees with no one running them show as unavailable in the picker.
- When the task status is **Backlog**, assigning **does not trigger the digital employee** — Backlog is a parking lot; the digital employee only gets enqueued once you move the task to Todo or In Progress.

## Assign from the CLI

The command-line equivalent:

```bash
multica issue assign MUL-42 --to alice
multica issue assign MUL-42 --to-id 5fb87ac7-23b5-4a7a-81fa-ed295a54545d
```

`--to` takes a member username or a digital employee name (fuzzy match). When names overlap — e.g. a digital employee `J` alongside `Cursor - J` — pass `--to-id <uuid>` instead, using the `user_id` (member) or `id` (digital employee) from `multica workspace member list --output json` / `multica agent list --output json`. UUID matching is strict and unambiguous, which is what you want from scripts and from digital employees driving the CLI. `--to` and `--to-id` are mutually exclusive.

Unassign:

```bash
multica issue assign MUL-42 --unassign
```

## What happens after assignment

When a non-Backlog task is assigned to a digital employee, Multica immediately does the following in the background:

1. Enqueues a `queued` `task` with priority inherited from the task, routed to the runtime where the digital employee lives.
2. The digital employee's daemon picks up the `task` on its next poll and transitions it to `dispatched`.
3. The digital employee starts working and the `task` moves to `running`; on completion it becomes `completed` or `failed`.
4. During execution the digital employee can change the task's status, post comments, and edit fields — these actions appear under the digital employee's identity.

**If the digital employee is offline**, the `task` waits in the queue — **it times out and fails after 5 minutes** with reason `runtime_offline`. For retryable sources (assign, @-mention, chat), Multica automatically re-enqueues it. See [**Tasks**](/tasks) for the full retry rules.

Assigning also auto-subscribes the digital employee to the task — but in Multica **digital employees do not receive inbox notifications** (only members do). This subscription is internal bookkeeping with no user-visible side effect.

## Reassign or unassign

When you change the assignee from Digital Employee A to Digital Employee B:

1. **Everything A has in flight is cancelled** — every `task` in `queued`, `dispatched`, or `running` state is marked `cancelled`.
2. **B is enqueued a new `task` immediately** (if the task is not in Backlog and B has an online runtime).

<Callout type="warning">
**Reassignment cancels every active `task` on this task — not just the old assignee's.** If another digital employee is working on this task because of an @-mention, its `task` is cancelled too. There is currently no UI action to cancel a single digital employee's `task` in isolation.
</Callout>

Unassigning (`--unassign` or picking "none" in the picker) marks all active `task` entries as `cancelled` and **does not enqueue a new one**. Existing subscriptions are not cleared automatically — the old assignee stays on the subscription list (but still receives no inbox notifications).

## Why only one active `task` per digital employee per task

**A single digital employee can have at most one `queued` or `dispatched` `task` on the same task at any time.** A unique index at the database level plus the claim logic enforces this — it prevents duplicate enqueues and concurrent executions overwriting each other.

But **different digital employees can work on the same task in parallel** — for example, Digital Employee A is the assignee and Digital Employee B is @-mentioned; both `task` entries can coexist, each running on its own runtime. See [**Tasks**](/tasks) for the full serial/concurrent rules.

## Next

- [**@-mention a digital employee in a comment**](/mentioning-agents) — a lighter trigger that leaves assignee and status untouched
- [**Digital departments**](/squads) — assign to a group of digital employees and let the leader decide who picks it up
- [**Chat**](/chat) — one-to-one conversation outside any task
- [**Autopilots**](/autopilots) — let digital employees start work automatically on a schedule
